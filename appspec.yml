version: 0.0
os: linux

files:
  - source: .
    destination: /var/www/html
    overwrite: true

hooks:
  AfterInstall:
    - location: scripts/install.sh
      runas: ubuntu

  ApplicationStart:
    - location: scripts/start.sh
      runas: ubuntu

